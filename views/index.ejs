<%- include('partials/head') %>

<%- include('partials/nav') %>

<main>

  <!-- ═══ HERO ═══ -->
  <section class="hero" id="hero">
    <div class="hero-bg">
      <div class="hero-grain"></div>
      <div class="hero-orb hero-orb-1"></div>
      <div class="hero-orb hero-orb-2"></div>
      <div class="hero-orb hero-orb-3"></div>
    </div>
    <div class="hero-border"></div>

    <div class="hero-content">
      <p class="hero-eyebrow reveal-up"><%= content.hero.eyebrow %></p>

      <h1 class="hero-title">
        <span class="hero-title-line reveal-up delay-1"><%= content.hero.headline %></span>
        <span class="hero-title-script script reveal-up delay-2"><%= content.hero.headlineAccent %></span>
      </h1>

      <p class="hero-sub reveal-up delay-3"><%= content.hero.subheadline %></p>

      <div class="hero-actions reveal-up delay-4">
        <a href="<%= content.hero.ctaPrimary.href %>" class="btn btn-terracotta">
          <%= content.hero.ctaPrimary.label %>
        </a>
        <a href="<%= content.hero.ctaSecondary.href %>" class="btn btn-ghost-light">
          <%= content.hero.ctaSecondary.label %>
        </a>
      </div>

      <div class="hero-scroll reveal-up delay-5">
        <div class="hero-scroll-line"></div>
        <span><%= content.hero.scrollLabel %></span>
      </div>
    </div>
  </section>

  <!-- ═══ MARQUEE ═══ -->
  <div class="marquee-wrap">
    <div class="marquee-track">
      <% for (let i = 0; i < 3; i++) { %>
        <% content.marquee.forEach(function(item) { %>
          <span class="marquee-item"><%= item %></span>
          <span class="marquee-sep">◈</span>
        <% }); %>
      <% } %>
    </div>
  </div>

  <!-- ═══ STATS ═══ -->
  <section class="stats-section">
    <div class="stats-grid">
      <% content.stats.forEach(function(stat) { %>
        <div class="stat-item reveal">
          <span class="stat-value" data-value="<%= stat.value %>"><%= stat.value %></span>
          <span class="stat-label"><%= stat.label %></span>
        </div>
      <% }); %>
    </div>
  </section>

  <!-- ═══ PHILOSOPHY ═══ -->
  <section class="philosophy" id="philosophy">
    <div class="philosophy-inner">
      <div class="philosophy-header">
        <p class="section-eyebrow reveal-up"><%= content.philosophy.eyebrow %></p>
        <h2 class="section-title reveal-up delay-1">
          <%= content.philosophy.title %>
          <em class="script-heading"><%= content.philosophy.titleAccent %></em>
        </h2>
        <p class="section-subtitle reveal-up delay-2"><%= content.philosophy.subtitle %></p>
      </div>

      <div class="pillars">
        <% content.philosophy.pillars.forEach(function(pillar, i) { %>
          <div class="pillar reveal delay-<%= i %>">
            <div class="pillar-icon"><%= pillar.icon %></div>
            <h3 class="pillar-title"><%= pillar.title %></h3>
            <p class="pillar-body"><%= pillar.body %></p>
            <div class="pillar-line"></div>
          </div>
        <% }); %>
      </div>
    </div>
  </section>

  <!-- ═══ COLLECTION ═══ -->
  <section class="collection" id="collection">
    <div class="collection-inner">
      <div class="collection-header">
        <p class="section-eyebrow reveal-up"><%= content.collection.eyebrow %></p>
        <h2 class="section-title reveal-up delay-1"><em><%= content.collection.title %></em></h2>
        <p class="section-subtitle reveal-up delay-2"><%= content.collection.subtitle %></p>
      </div>

      <div class="cards">
        <% content.collection.items.forEach(function(item, i) { %>
          <article class="card reveal delay-<%= i % 3 %>">
            <div class="card-img card-img--<%= item.color %>">
              <div class="card-img-label"><%= item.meta %></div>
              <div class="card-img-overlay">
                <p class="card-img-desc"><%= item.desc %></p>
                <a href="#" class="btn btn-ghost-sm">View Piece</a>
              </div>
            </div>
            <div class="card-body">
              <span class="badge badge--<%= item.badge.style %>"><%= item.badge.label %></span>
              <h3 class="card-title"><%= item.title %></h3>
              <p class="card-meta"><%= item.meta %></p>
              <div class="card-footer">
                <span class="card-price"><%= item.price %></span>
                <button class="card-btn" aria-label="Add to wishlist">♡</button>
              </div>
            </div>
          </article>
        <% }); %>
      </div>

      <div class="collection-cta reveal">
        <a href="#" class="btn btn-secondary">View Full Collection</a>
      </div>
    </div>
  </section>

  <!-- ═══ EDITORIAL SPLIT ═══ -->
  <section class="editorial" id="editorial">
    <div class="editorial-inner">
      <div class="editorial-left reveal-left">
        <p class="section-eyebrow"><%= content.editorial.eyebrow %></p>
        <blockquote class="editorial-quote"><%= content.editorial.quote %></blockquote>
        <cite class="editorial-cite"><%= content.editorial.cite %></cite>
      </div>
      <div class="editorial-right reveal-right">
        <div class="editorial-panel">
          <div class="editorial-grain"></div>
          <div class="editorial-orb"></div>
          <p class="editorial-body"><%= content.editorial.body %></p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ TESTIMONIALS ═══ -->
  <section class="testimonials-section">
    <div class="testimonials-inner">
      <p class="section-eyebrow center reveal-up">Worn & Loved</p>
      <h2 class="section-title center reveal-up delay-1">The Women <em>Behind the Drape</em></h2>

      <div class="testimonials-track-wrap">
        <div class="testimonials-track" id="testimonials-track">
          <% content.testimonials.forEach(function(t) { %>
            <div class="testimonial-card">
              <p class="testimonial-quote"><%= t.quote %></p>
              <div class="testimonial-author">
                <div class="testimonial-avatar"></div>
                <div>
                  <p class="testimonial-name"><%= t.author %></p>
                  <p class="testimonial-title"><%= t.title %></p>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
        <div class="testimonials-nav">
          <button class="t-nav-btn" id="t-prev">←</button>
          <div class="t-dots">
            <% content.testimonials.forEach(function(_, i) { %>
              <span class="t-dot <%= i === 0 ? 'active' : '' %>" data-idx="<%= i %>"></span>
            <% }); %>
          </div>
          <button class="t-nav-btn" id="t-next">→</button>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ PROCESS ═══ -->
  <section class="process-section" id="story">
    <div class="process-inner">
      <div class="process-header">
        <p class="section-eyebrow reveal-up"><%= content.process.eyebrow %></p>
        <h2 class="section-title reveal-up delay-1">
          <%= content.process.title %>
          <em class="script-heading"><%= content.process.titleAccent %></em>
        </h2>
      </div>

      <div class="process-steps">
        <% content.process.steps.forEach(function(step, i) { %>
          <div class="process-step reveal delay-<%= i %>">
            <div class="step-num"><%= step.num %></div>
            <div class="step-content">
              <h3 class="step-title"><%= step.title %></h3>
              <p class="step-body"><%= step.body %></p>
            </div>
            <% if (i < content.process.steps.length - 1) { %>
              <div class="step-connector"></div>
            <% } %>
          </div>
        <% }); %>
      </div>
    </div>
  </section>

  <!-- ═══ NEWSLETTER ═══ -->
  <section class="newsletter" id="contact-form">
    <div class="newsletter-inner">
      <div class="newsletter-bg">
        <div class="newsletter-orb newsletter-orb-1"></div>
        <div class="newsletter-orb newsletter-orb-2"></div>
      </div>

      <p class="section-eyebrow center reveal-up"><%= content.newsletter.eyebrow %></p>
      <h2 class="section-title center reveal-up delay-1"><em><%= content.newsletter.title %></em></h2>
      <p class="section-subtitle center reveal-up delay-2"><%= content.newsletter.subtitle %></p>

      <form class="newsletter-form reveal-up delay-3" onsubmit="handleSubscribe(event)">
        <div class="newsletter-input-wrap">
          <input
            type="email"
            id="newsletter-email"
            placeholder="<%= content.newsletter.placeholder %>"
            aria-label="Email address"
            required
          >
          <button type="submit" class="btn btn-terracotta"><%= content.newsletter.cta %></button>
        </div>
        <p class="newsletter-note">No spam. Ever. Unsubscribe anytime.</p>
      </form>
    </div>
  </section>

</main>

<%- include('partials/footer') %>

<!-- GSAP CDN -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>

<!-- App JS -->
<script src="/js/main.js"></script>

</body>
</html>
